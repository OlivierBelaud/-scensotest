const sillyname = require('sillyname');
const apple = require('@olivierbelaud/apple');
const banana = require('@olivierbelaud/banana');
console.log(`grocery and ${sillyname()}`);
console.log(apple);
console.log(banana);